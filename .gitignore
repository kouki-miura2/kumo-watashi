# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# dotenv environment variable files
.env
.env.*
!.env.example

# Wrangler (Cloudflare Workers)
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example
worker-configuration.d.ts

# Contains this repo's own D1 database_id / R2 bucket_name — not exploitable alone, but this
# repo is public, so treated as sensitive anyway (see .claude/skills/check-secrets). Copy
# wrangler.jsonc.example to wrangler.jsonc and fill in real values locally.
apps/backend/wrangler.jsonc

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json
