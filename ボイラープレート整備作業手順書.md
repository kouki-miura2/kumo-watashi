# ボイラープレート整備作業手順書

`AGENTS.md` に記載した構成を実現するための作業手順。現状はリポジトリ雛形(pnpm workspace + Vite+ + Hono/Vue のデフォルトテンプレート)のみが存在する状態からの整備手順を示す。

## 方針

- コード記述量を増やさないことを優先する。フレームワーク標準機能で済むものを自前実装・自前抽象化に置き換えない。
- API のリクエスト/レスポンス型は手書き・共有パッケージ化しない。Hono RPC (`hc<AppType>()`) による型推論に一本化し、`packages/types` のような型共有専用パッケージは作らない。
- 汎用化・抽象化は、実際に複数箇所で必要になった時点で行う(先回りして層を増やさない)。

## 前提

- パッケージマネージャ: pnpm workspace (`apps/*`, `packages/*`)
- ツールチェーン: Vite+ (`vp`)
- 既存構成: `apps/backend`(Hono テンプレート。現状は Cloudflare Workers 用エントリポイントのみ), `apps/frontend`(Vite+ デフォルトテンプレート), `packages/utils`(TypeScript パッケージ雛形のみ)

## 1. packages/utils の実装

1. 雛形の `fn()` (`src/index.ts`) を削除する。
2. 日時操作ユーティリティを実装する。
   - `src/date/` 配下に関数を追加し、`src/date/xxx.test.ts` を co-location する。
   - フォーマット・パース・加減算など、両アプリで共通に使う操作を対象にする。
3. ロガーを実装する。
   - `src/logger/` 配下に `console.*` をラップする実装を追加する(ログレベル、プレフィックス、実行環境による出力制御)。
   - `apps/backend`(Workers / Node)と `apps/frontend`(ブラウザ)の両方で動く実装にする。
4. `src/index.ts` から日時関数・ロガーを re-export する。
5. `package.json` のプレースホルダ(`author`, `repository`, `homepage` 等)を実リポジトリの値に更新する。

## 2. apps/backend の実装

1. `packages/utils` を依存関係に追加する。
2. ランタイムを分離する。
   - ルーティング/ミドルウェア定義を `src/app.ts` に runtime 非依存な形で実装する(Cloudflare Workers 固有 API・Node 固有 API を持ち込まない)。
   - Cloudflare Workers 用エントリポイント `src/worker.ts` を作成し、`wrangler.jsonc` の `main` をこちらに向ける(既存の `src/index.ts` を改名・分離)。
   - Node.js 用エントリポイント `src/server.ts` を作成し、`@hono/node-server` で `app.ts` を起動する。
   - `package.json` に Node 用スクリプト(例: `dev:node`, `start`)を追加し、`dev` / `deploy`(wrangler)と併存させる。
3. `src/repositories/` ディレクトリを作成し、Repository パターンの規約を確立する。
   - インターフェースを `*.interface.ts` として定義する。
   - サンプル実装として in-memory / mock 実装を1つ用意し、DAO 差し替えの方法を示す。
   - Cloudflare Workers 向け(D1/KV等)と Node.js 向け(任意の DB クライアント等)で異なる DAO 実装を用意できることが分かる構成にする。
4. 各エントリポイント(`worker.ts` / `server.ts`)で、インターフェースに対して実行環境ごとの具象実装を注入する配線を実装する。
5. `packages/utils` のロガーを Hono のミドルウェアとして組み込む(リクエストログ等)。
6. サンプルエンドポイントを Repository 経由のデータ取得に置き換える。
7. `src/app.ts` からアプリ本体の型(`export type AppType = typeof app`)を export し、Hono RPC の契約として `apps/frontend` から参照できるようにする。DTO 用の型を別途手書きしない。

## 3. apps/frontend の実装

1. 雛形由来のサンプルコード(`counter.ts`, `assets/hero.png` 等の説明用アセット)を削除する。
2. Vue 3 を導入し、`src/main.ts` を `createApp` ベースの構成に置き換える。
3. Vuetify 4 を導入する。
   - プラグインとして `main.ts` に登録する。
   - テーマ設定の雛形を用意する。
4. vue-router を導入する。
   - `src/router/index.ts` にルーティング定義を作成する。
   - サンプルページを1つ以上用意する。
5. Pinia を導入する。
   - `src/stores/` ディレクトリを作成し、サンプルストアを1つ用意する。
6. `packages/utils` を依存関係に追加し、サンプルページ上でロガーを利用する。
7. API クライアントを Hono RPC で用意する。
   - `apps/backend` を devDependency として追加し、`AppType` を型のみ import する(`import type { AppType } from "backend/src/app"` 等)。
   - `src/api/` などに `hc<AppType>(baseUrl)` を1箇所で生成する薄いラッパーのみを置く(型定義やDTOの手書きは行わない)。
8. Vue 3 + Vuetify 4 + vue-router + Pinia + Hono RPC クライアントが連携して動作するサンプル画面を作成し、動作確認する。

## 4. 共通設定の整備

1. ルートの `tsconfig.json` で `packages/utils` への path alias / project references を整理する。
2. `apps/frontend` から `apps/backend` への型のみ import が型チェック上問題なく解決できるか確認する(`verbatimModuleSyntax` / `isolatedModules` 等の設定を確認)。
3. Lint / Format ルール(Oxlint / Oxfmt)を必要に応じて調整する。
4. 実装内容と `AGENTS.md` の記載に乖離がないか確認し、差分があれば `AGENTS.md` を更新する。

## 5. 動作確認

1. `vp install`
2. `vp check`(format / lint / typecheck)
3. `vp run -r test`
4. `vp run -r build`
5. `vp run backend#dev`, `vp run frontend#dev` でそれぞれ起動確認
6. `apps/backend` の Node.js エントリポイント(`dev:node` 等)でも起動確認
7. フロントエンドから Hono RPC 経由でバックエンドの型が正しく推論・補完されることを確認する

## 完了条件チェックリスト

- [ ] `packages/utils` に日時操作関数とロガーが実装され、テストが存在する
- [ ] `apps/backend` が Repository パターンで実装され、DAO を差し替えられる構成になっている
- [ ] `apps/backend` が Cloudflare Workers(`wrangler dev`)と Node.js(`server.ts` 起動)の両方で起動できる
- [ ] `apps/backend` が `AppType` を export し、Hono RPC の契約として機能している
- [ ] `apps/frontend` が `hc<AppType>()` 経由でバックエンドAPIの型を手書きなしに利用できる
- [ ] `apps/frontend` が Vue 3 + Vuetify 4 + vue-router + Pinia で動作する最小画面を持つ
- [ ] `packages/types` に相当する型共有パッケージが存在しない(Hono RPC に一本化されている)
- [ ] `vp run ready`(format / lint / typecheck / test / build)が成功する
